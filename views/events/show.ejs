<%- include('../partials/header.ejs') %> 
<body>

  <div class="container" style=" width:50%; word-wrap: break-word;">
     <div class="row">
      <div class="text-center" style="padding-top: 90px;">
        <div><img height="300" width="300" src="<%= event.eventImagePath %>"></div>
        <h1><%= event.title %></h1>
        <p class="event_comment_description text-center" ><%= event.description %></p>
        <h4><%= event.startingDate %></h4>
        <h4><%= event.closingDate %></h4>
      </div>
    </div>
  </div>
  <div class="container" style=" width:55%; word-wrap: break-word;">
<div class="text-center">
<table class="table table-striped table-dark">
	<thead>
	  <tr>
		<th scope="col">Comments</th>
		<th scope="col">Attendants</th>
		<th scope="col">Are you coming? Click Yes</th>
	  </tr>
	</thead>
	<tbody>
	  <tr>
		<td><% const counter2 = event.eventcomments.length >= 2 ? "Comments" : "Comment"; %>
			<button class="show-comments btn btn-secondary" ><%= event.eventcomments.length + " " + counter2 + "" %></button>
		  <% %>
		</td>
		<td class="btn btn-outline-dark" style="font-weight: bold"><% const counter = event.going + event.coming_with >= 2 ? "People going" : "Person going"; %>
			<button id="show-attendance " ><%= event.going + event.coming_with + " " + counter + "" %></button>
		</td>
		<td>
			<label for="click" class="click-me btn btn-success" href="#" data-id="<%=event.id%>">I'm Going</label>
		</td>
	</tr>
</tbody>
</table> 


<div class="center">
 	<input type="checkbox" id="click">
	<div class="content">
		<div class="popupheader">
		  <h2>HALLELUYAH! GOD BLESS YOU</h2>
		  <label for="click" class="fa fa-times"></label>
		</div>
		<h3 id="thankyou">Are you coming with someone? </h3> 
		<div class="columnContainer">
			<button type="button" id="yes" class="btn btn-success col-md-5">Yes</button>
			<button type="button" id="no" class="btn btn-info col-md-5 col-md-offset-1">No</button>
		</div>
		<div id="coming_with"> choose how many people you want to come with</div>
		<form action="/events/<%=event.id%>/coming_with" method="POST" id="my_with">
			<select name="coming_with" id="chooseNumber">
				<option>Choose a number</option>
			</select>
			<button id="send_coming_with" type="submit">send</button>
		</form >
			<div class="line"></div>
			<label for="click" class="close-btn">Close</label>
	</div>
</div>

</div>
    <div class="postcomments text-center" ><% event.eventcomments.forEach(item => { %>
      <ul >
        <li><%= item.description %></li>
        <a class="delete-event-comment" href="#" data-id="<%=event.id%>/<%=item._id%>">Delete</a>
      </ul>
    <% });%>
    </div>
</div>




<!------------container------->
</br>
</br>

  <div id="wrap">
  <p>Your email address will not be published. Required fields are marked *</p>  
  <div id='form'>
    <div class="row">
      <div class="col-md-12">
        <form action="/events/<%=event._id%>/eventcomment" method="post" id="commentform">
          <div id="comment-name" class="form-row">
            <input type = "text" placeholder = "Name (required)" name="name"  id="name" >
          </div>
          <div id="comment-message" class="form-row">
            <textarea name="description" placeholder = "Message" id="comment" ></textarea>
          </div>
          <a href="#"><input type="submit" name="dsubmit" id="commentSubmit" value="Submit Comment"></a>
          <p1><b>Notify me when new comments are added.</b></p1>
        </form>
      </div>
    </div>
  </div>
  </body>