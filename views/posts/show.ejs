<div class="text-center">
  <li><img height="400" width="400" src="<%= post.coverImagePath %>"></li>
  <h1><%= post.title %></h1>
  <p><%= post.description %></p>
  <h4><%= post.createdAt %></h4>
  <% const counter = post.comments.length >= 2 ? "Comments" : "Comment"; %>
  <button class="show-comments"><%= post.comments.length + " " + counter + "" %></button>
  <% %>
  <div class="postcomments" ><% post.comments.forEach(item => { %>
    <ul >
      <li><%= item.description %></li>
      <a class="delete-comment" href="#" data-id="<%=post.id%>/<%=item._id%>">Delete</a>

    </ul>
  <% });%>
  </div>
</div>


<!------------container------->

<div class="container">

<!-------Wrap------------>
<div id="wrap">

<p>Your email address will not be published. Required fields are marked *</p>

<!------------Form Start---------->

<div id='form'>
<div class="row">
<div class="col-md-12">

  <form action="/posts/<%=post._id%>/comment" method="post" id="commentform">

<div id="comment-name" class="form-row">
<input type = "text" placeholder = "Name (required)" name="name"  id="name" >
</div>
<div id="comment-message" class="form-row">
<textarea name="description" placeholder = "Message" id="comment" ></textarea>
</div>
<a href="#"><input type="submit" name="dsubmit" id="commentSubmit" value="Submit Comment"></a>
<p1><b>Notify me when new comments are added.</b></p1>
</form>

</div>
</div>
</div>
</div>